#include <bits/stdc++.h>
using namespace std;

int main()
{
    int power = pow(2,3);
    int sqaure = sqrt(25);
    int sealling = ceil(4.2);
    int flooring = floor(4.2);
    int log = log2(512);
    cout << power << endl;
    cout << sqaure << endl;
    cout << sealling << endl;
    cout << flooring << endl;
    cout << log << endl;
    return 0;
}